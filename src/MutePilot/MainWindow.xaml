<Window x:Class="MutePilot.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MutePilot"
        Width="620"
        Height="720"
        MinWidth="520"
        MinHeight="600"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource WindowBackgroundBrush}"
        Foreground="{StaticResource PrimaryTextBrush}"
        Loaded="MainWindow_Loaded">
    <Grid Margin="28,24,28,18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,22">
            <TextBlock FontSize="28"
                       FontWeight="SemiBold"
                       Text="MutePilot" />
            <TextBlock Margin="0,6,0,0"
                       FontSize="13"
                       Foreground="{StaticResource SecondaryTextBrush}"
                       Text="Custom hotkey-based Windows audio muting" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Padding="18"
                    Background="{StaticResource PanelBackgroundBrush}"
                    BorderBrush="{StaticResource PanelBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="8">
                <StackPanel>
                    <TextBlock FontSize="17"
                               FontWeight="SemiBold"
                               Text="Master Audio" />
                    <TextBlock x:Name="MasterAudioStatusText"
                               Margin="0,8,0,14"
                               FontSize="13"
                               Foreground="{StaticResource SecondaryTextBrush}"
                               Text="현재 상태: 확인 중"
                               TextWrapping="Wrap" />
                    <Button x:Name="MasterMuteButton"
                            Width="160"
                            Height="32"
                            HorizontalAlignment="Left"
                            Click="MasterMuteButton_Click"
                            Content="상태 확인 중"
                            IsEnabled="False" />
                    <TextBlock x:Name="AudioErrorText"
                               Margin="0,12,0,0"
                               FontSize="12"
                               Foreground="#FCA5A5"
                               TextWrapping="Wrap"
                               Visibility="Collapsed" />
                </StackPanel>
            </Border>

            <Border Grid.Row="1"
                    Margin="0,16,0,0"
                    Padding="18"
                    Background="{StaticResource PanelBackgroundBrush}"
                    BorderBrush="{StaticResource PanelBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock FontSize="17"
                                       FontWeight="SemiBold"
                                       Text="Applications" />
                            <TextBlock Margin="0,5,12,0"
                                       FontSize="12"
                                       Foreground="{StaticResource SecondaryTextBrush}"
                                       Text="현재 기본 출력 장치의 활성 오디오 세션"
                                       TextWrapping="Wrap" />
                        </StackPanel>

                        <Button x:Name="ApplicationRefreshButton"
                                Grid.Column="1"
                                Width="84"
                                Height="30"
                                VerticalAlignment="Top"
                                Click="ApplicationRefreshButton_Click"
                                Content="새로고침" />
                    </Grid>

                    <TextBlock x:Name="ApplicationErrorText"
                               Grid.Row="1"
                               Margin="0,12,0,0"
                               FontSize="12"
                               Foreground="#FCA5A5"
                               TextWrapping="Wrap"
                               Visibility="Collapsed" />

                    <ScrollViewer Grid.Row="2"
                                  Margin="0,14,0,0"
                                  VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <TextBlock x:Name="ApplicationEmptyText"
                                       Padding="0,14"
                                       HorizontalAlignment="Center"
                                       FontSize="13"
                                       Foreground="{StaticResource SecondaryTextBrush}"
                                       Text="활성 애플리케이션 오디오 세션이 없습니다."
                                       Visibility="Collapsed" />

                            <ItemsControl x:Name="ApplicationsItemsControl">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Margin="0,0,0,10"
                                                Padding="12"
                                                Background="#222630"
                                                BorderBrush="{StaticResource PanelBorderBrush}"
                                                BorderThickness="1"
                                                CornerRadius="6">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto" />
                                                    <RowDefinition Height="Auto" />
                                                    <RowDefinition Height="Auto" />
                                                </Grid.RowDefinitions>

                                                <TextBlock Grid.Row="0"
                                                           Grid.Column="0"
                                                           FontSize="14"
                                                           FontWeight="SemiBold"
                                                           Text="{Binding ApplicationName}" />
                                                <TextBlock Grid.Row="1"
                                                           Grid.Column="0"
                                                           Margin="0,5,12,0"
                                                           FontSize="11"
                                                           Foreground="{StaticResource SecondaryTextBrush}"
                                                           Text="{Binding ProcessIdText}"
                                                           TextWrapping="Wrap" />
                                                <TextBlock Grid.Row="2"
                                                           Grid.Column="0"
                                                           Margin="0,4,12,0"
                                                           FontSize="12"
                                                           Foreground="{StaticResource SecondaryTextBrush}"
                                                           Text="{Binding StatusText}" />
                                                <Button Grid.Row="0"
                                                        Grid.RowSpan="3"
                                                        Grid.Column="1"
                                                        Width="92"
                                                        Height="32"
                                                        VerticalAlignment="Center"
                                                        Click="ApplicationMuteButton_Click"
                                                        Content="{Binding ToggleButtonText}"
                                                        Tag="{Binding ApplicationKey}" />
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <TextBlock Grid.Row="2"
                   Margin="0,18,0,0"
                   HorizontalAlignment="Center"
                   FontSize="12"
                   Foreground="{StaticResource SecondaryTextBrush}"
                   Text="Made by 유성우" />
    </Grid>
</Window>
